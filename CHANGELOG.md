# CHANGELOG — deepAStock 深度A股交易

> 版本日志按时间倒序。`v1.0.0` 为首次发布版本。

## v1.3.2 (2026-09-18) — K线默认缩放 + 板块合并 + 回测专业指标 + 宏观美债金银

### 修复
- **K线默认显示最近90根**：HQChartKline 组件 `dataZoom.start` 从"显示全部"改为默认缩放到最近90根K线，用户可自由拖拽扩展。
- **板块资金流+涨停排行合并**：原"板块资金流明细"和"板块涨停/跌停排行（近7交易日）"两个独立卡片合并为一个"板块资金流·涨停/跌停排行"表格，点击"加载7日累计"后显示7日涨停/跌停汇总列；减少页面冗余。

### 新增
- **回测专业指标**（对标专业量化工具）：
  - 新增 Sharpe Ratio、Sortino Ratio、Calmar Ratio 风险调整收益指标
  - 新增 盈亏比（Profit Factor）、平均持仓天数、最大连赢/连亏次数、平均盈利/亏损金额
  - 新增 最大回撤起止日期 + 修复天数
  - 新增 月度收益网格表（按月展示收益率）
  - 后端 `_finish_metrics` 全面增强，前端 metricCards 扩展至16项指标
- **宏观数据新增美债+贵金属**：
  - 美国国债5年收益率（`us_treasury_5y`）
  - 美国国债30年收益率（`us_treasury_30y`）
  - 黄金价格（伦敦金/上海金交所基准价，`gold_price`）
  - 白银价格（`silver_price`）
  - 新增对应解析器 `_parse_treasury_5y`/`_parse_treasury_30y`/`_parse_gold`/`_parse_silver`
  - 每日缓存一次，不频繁更新

### 其他
- 版本号升级 v1.3.1 → **v1.3.2**（前端 package.json 同步）。

## v1.3.1 (2026-09-18) — 复盘趋势图 + 回测增强 + 龙虎榜聚合 + 自选优化

### 新增
- **复盘涨停/跌停趋势折线图**：新增 `GET /api/v1/replay/trend` 接口，返回近一周涨停数、跌停数、连板率、断板率；Replay.vue 新增柱状+折线混合图（涨停/跌停柱状、连板率/断板率折线），直观展示情绪周期变化。
- **龙虎榜按游资席位聚合**：复盘页龙虎榜区域重构为「游资席位聚合视图」——按标签（拉萨天团/机构专用/北向资金/温州帮/杭州帮/成都帮/佛山帮/宁波敢死队/章盟主/赵老哥/炒股养家/小鳄鱼/溧阳路/上海超短等）分组，每组显示操作个股、净买额、营业部；原龙虎榜个股表明细移至下方。
- **次日选股池形态识别**：`_detect_pattern` 新增 5 种 K 线形态检测——十字星反转、弱转强、二次回踩支撑、底部反转、连板晋级；前端显示形态标签（红色 tag）。
- **策略回测 3 个新内置策略**：RSI 超卖反弹（参数：周期/超卖线/超买线）、MACD 金叉死叉（快线EMA/慢线EMA/信号线）、布林带突破（均线周期/标准差倍数），均为完整 Python 代码 + 参数 schema。
- **回测收益对比沪深300**：回测完成后自动拉取 SH000300 K 线数据，归一化后叠加到净值曲线图上（虚线），直观对比策略与大盘表现。
- **回测最大回撤标记**：净值曲线图上三角标记回撤起点、pin 标记回撤底、圆点标记修复位置，显示回撤百分比和修复天数。
- **自选分组删除按钮**：非"默认分组"的自选分组显示 ✕ 删除图标，点击确认后删除分组（最近查看和默认分组不可删除）。

### 修复
- **回测股数非整数问题**：`_simulate` 和 `_simulate_portfolio` 买入股数改为 `int(spend/fill/100)*100`（100 股整手），不足 100 股不建仓，符合 A 股交易规则。
- **回测策略未来函数检测**：`_check_future_function` 正则扫描 `bars[i+k]`、`closes[i+k]` 等模式，编译时即拦截使用未来数据的策略代码。
- **新建策略无临时位置**：点击"新建策略"立即在列表顶部插入临时条目（显示"新策略（未保存）"），保存后替换为真实策略，避免用户误以为策略已创建。
- **自选自动刷新干扰 K 线缩放**：`loadKline` 改为增量更新（diff 尾部 K 线数据，原地修改而非替换数组引用），加防重入锁，30 秒定时刷新不再重置用户缩放/拖拽状态。
- **复盘 `target must be an object` 错误**：`sortedLadder` computed 加防御性 null/类型检查，模板中 `Object.keys(sortedLadder)` 改为 `Object.keys(sortedLadder || {})`。
- **复盘趋势跌停数修正**：`get_trend` 跌停数从 `distribution.limit_down` 获取（全市场跌停家数），而非 `distribution.down_count`（全市场下跌家数）。

### 变更
- **复盘龙虎榜 UI 重构**：原「龙虎榜 + 席位游资」平铺布局改为「游资席位聚合视图 + 上榜个股明细」上下结构，按席位标签分组展示，每组显示操作个股和净额汇总。
- **次日选股池增强**：`_enrich_stock_pool` 新增 K 线形态识别，每个入选股票附带 `pattern_tags`（形态标签列表）和针对性建议。
- **自选 K 线刷新策略优化**：从"清空→重新加载"改为"增量更新"，保留用户交互状态。

### 其他
- 版本号升级 v1.2.2 → **v1.3.1**（前端 package.json 同步）。

## v1.2.1 (2026-09-16) — 复盘UI游资化 + 同花顺兑底源 + H5移动适配

### 新增
- **复盘页「游资视角」重构**（Replay.vue）：连板梯队（maxBoard 最高连板高亮）、龙虎榜聚合、席位游资标签、次日选股池、Agent 审评——均真实数据判断。
- **同花顺（hithink）第4兑底数据源**：manager.py 注册“hithink”，作为 sina→tencent→akshare 失败后的独立回退路，API Key 从数据库设置读（不显示/不回显/不入 Git/不入日志）。
- **宏观新增「美联储」组**（fed_funds/us_cpi/us_nfp）、个股日K 新增 **MA250 年均线**（浅灰线）、板块资金流补 **跌停家数列**。
- **H5 移动适配**：宏观页、复盘页、首页均新增 @media 820/480 两档。
- **start.bat 启动前清理残留进程**（cleanup_stale 接入 dev_up.py），避免旧 uvicorn/vite 占用 8000/5173 导致界面假死。

### 修复
- **复盘页额外纯化性修复**：RSS 标题 HTML 清洗、消息滚动最多一行；后端程序启动前 start.bat 自动清理残留进程。

### 其他
- 版本号 v1.2.0 → **v1.2.1**（前端 package.json 同步）。

## v1.2.0 (2026-09-16) — RSS 标题清洗 + 消息滚动最多换一行 + RSSHub 无浏览器/AMD 无 GPU 方案沉淀

### 修复
- **RSS / RSSHub 新闻标题清洗**：同步/订阅 RSSHub 的部分源（如媒体快讯、行业要闻类）标题带 HTML 片段（`<span class="section-news">…</span>`, `<br/>`, `&nbsp;` 等），现已做**双端清洗**——后端解析入库时统一去标签 + `&#160;/&nbsp` 实体解码 + 空白折叠（`clean_text`），读取接口再兜底一次（覆盖历史脏数据）；前端消息滚动显示时再次 `stripHtml` 兜底，不再出现按钮/标签带着尖括号 `1. 媒体：…` 之类的原始 HTML。涉及服务端 `parser.py` / `service.py` 与前端 `Home.vue`。
- **消息滚动长消息最多换一行**：平台/RSS 长标题不再单行硬截断也不无限换行，改为 `-webkit-line-clamp: 2`（最多两行、第二行可容纳完整文案后截断），滚动卡片高度稳定不跳动。

### 其他
- 版本号升级 v1.1.7 → **v1.2.0**（前端 package.json 同步）。
- **RSSHub 无浏览器 / AMD 无 GPU 环境方案（仅供运维参考，不影响多数数据源）**：
  - 若本机 RSSHub 用 `docker run diygod/rsshub` 基础镜像，**视频/图像类源**（如 bilibili、Instagram、YouTube 等需要浏览器渲染的源）会因缺少 Chromium/Playwright 报 `browser.launch: executable doesn't exist`。建议针对需要浏览器的源改用 `diygod/rsshub:chromium-bundled` 镜像（自带 Playwright Chromium，约多 1~2GB），仅该镜像运行时可正常渲染；其余纯文本/行情源不受影响。
  - **AMD / 无独立 GPU** 环境（如老 APU 或核显机器）：Chromium 渲染 RSSHub 源时默认会尝试硬件加速，AMD 无独显或驱动未就绪时可能出现渲染卡死/超时。方案：给 chromium-bundled 容器加 `PLAYWRIGHT_WS_ENDPOINT` 外挂浏览器服务（如 `browserless/chrome` 容器），或在宿主机设 `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` 后仅启动浏览型源时用独立容器。绝大多数行情/文本类数据源始终走轻量 HTTP 解析，**不需要浏览器、不需要 GPU**。本版本未改动任何数据源默认行为，仅记录此专项说明。

## v1.1.7 (2026-09-15) — 大盘股价异动 + 复盘历史可视化 + 自选股改版 + MA250

### 新增功能
- **大盘看板「实时股价异动」板块**（人气股票TOP10下方，同风格卡片）：基于腾讯实时涨速榜，分「🚀 快速拉升 TOP5 / ⚡ 快速下挫 TOP5」两列展示，每项含 涨速(涨跌幅/分、红绿标注)、现价、涨跌幅、量比、换手率、成交额；点击卡片进入个股详情。新接口 `GET /api/v1/market/price-movers`（60s 缓存），并列滚动展示。
- **复盘页「历史复盘」日期选择器**（顶部下拉）：可从历史复盘列表（`POST /api/v1/replay/history`）任选日期查看对应日期的完整复盘报告（`GET /api/v1/replay/{date}`），不再只能看最新一份；支持「刷新」保留当前查看日期；无记录该书显示「该日期暂无复盘记录」。
- **自选股列表改版**：个股卡片新增 今开/最高/最低/成交量/成交额 字段，列表可容纳更长、更完整的自选与最近查看记录。
- **自选股 AI 分析按钮常驻**：点击任意智能体分析进行流式输出时，其余智能体按钮不再消失，可连续多次触发不同智能体；流式输出与结论卡片分开展示。
- **个股K线新增 MA250（250日均线）**，与 MA5/10/20/60 一并展示，配色区分，便于识别长期趋势中枢。

### 修复
- 自选股 AI 分析在流式期间按钮消失的问题。
- 大盘资金流向移除「逐日」视图（仅保留今日分时），避免展示含义模糊的历史分时数据。

### 其他
- 后端版本号升级 v1.1.6 → v1.1.7。

## v1.1.6 (2026-09-15) — 宏观数据页 + 智能体 curl 一键导入 + 自选页体验优化

### 新增功能
- **宏观数据页**（导航「行情 → 宏观数据」，`/macro`）：替代原占位页，展示经济指标**每日快照**而非实时行情——CPI / PPI / PMI / GDP / 社会消费品零售 / 出口 / 进口 / 贸易顺差 / 城镇调查失业率 / 新建商品住宅价格 / 房地产投资 / M2 同比 / 居民储蓄存款，共 13 项；以卡片展示最新值、与前值对比、近 12 期迷你走势图，按 价格 / 景气 / 增长 / 消费 / 贸易 / 就业 / 房价 / 货币 / 存款 分组；数据源自 akshare（jin10 / 统计局口径），**每天抓取一次并缓存**（`data/cache/macro_daily.json`），页面支持「刷新」（读缓存）与「强制重抓」（`/api/v1/market/macro?refresh=1`）。
- **智能体 curl 一键导入**（智能体页 → 新建/编辑弹窗）：粘贴任意 OpenAI / Anthropic / 其他兼容 API 的 **curl 命令**，点「解析 curl」自动提取并填充 服务商 / API 地址 / API Key / 模型名（`POST /api/v1/agents/parse-curl`）；支持 `Authorization: Bearer` / `x-api-key` 两种取 Key 方式，兼容 `--data`/`--data-raw`/`--json`、`https://` 裸 URL、Gemini 路径内模型写法，已测 OpenAI / Anthropic / DeepSeek / 智谱 / Ollama / Gemini 六类。
- **自选股页「综合建议」智能体按钮更醒目**：三个 AI 按钮加 tooltip 说明（投研 / 短线 / 波段）与刷新图标，明确可点击重新分析。
- **最近查看列表显示实时行情**：最近查看模式与自选模式一样轮询刷新最新价格，不再停留静态价。

### 变更
- 「宏观数据」由占位页升级为真实页面，左侧导航标签移除「敬请开放」后缀；`/macro` 占位感知逻辑移除。

## v1.1.5 (2026-09-14) — AI 流式分析 + 可编程策略回测（单只 / 自选组合）

### 新增功能
- **AI 分析流式输出（SSE）**：个股/看板 AI 结论改为边生成边推送（`POST /api/v1/brainstorm/{symbol}/{agent_type}/stream`），自选股页流式渲染，首个 token 更快，不再整段干等。
- **策略回测**（导航「回测」）：
  - 内置「均线金叉死叉」策略，周期可调，支持区间选择 + 初始资金 + 权益净值曲线 + 交易明细表。
  - **策略管理**：策略全部落库（`strategy_configs` 表），可新建 / 编辑 / 复制 / 删除 / 停用；**自己写策略**——代码契约 `run(bars, params)`，环境内置 `sma/ema`，返回买卖信号与资金占比；参数 schema 驱动回测表单，「测试代码」用合成 K 线秒级验证语法与信号产出。
  - **自选组合回测**：自己挑多只股票（最多 20 只）一次回测，策略按每只股票独立出信号、组合资金统一模拟（按日期对齐算净值、期末持仓按收盘估值），明细带「标的」列；支持「从自选股导入」把自选分组一键加入组合。
  - 内置策略**单只 / 组合双模式**：同一份代码以 `list`（单只）或 `dict {symbol: K线}`（组合）两种入参运行，组合模式新增「组合建仓比例」参数控制注资占比。
- **模拟交易账户 AI 分散化**：候选池按账户轮转 + 投研 / 短线 / 波段风格评分修正，不再所有 AI 扎堆买入同一只票。
- **全局消息通知多条同时弹出**：同一时间到达的新闻不再一条条排队覆盖，而是右上角纵向叠放同时展示，每条 15 秒自动消失（点击跳原文 / 主动关闭）；RSS 订阅推送只对 重要(1) / 普通(2) 级别弹窗，其他级别不打扰。

### 修复
- **高价股回测 0 交易**：按 100 股整手建仓导致茅台等一手市值超本金的标的永远买不起 → 改为浮点份额连续持仓。
- 自选股页分组 / 最近查看上下文丢失：点「最近查看」里的个股或从其他页面带 `?symbol=` 跳转过来时被强切到默认分组 → 现在保持当前分组 / 最近查看不变，并用 localStorage 记住最后所处位置。

### 变更
- 导航重构为**分组大菜单**：行情（自选股 / 每日复盘 / 宏观数据·敬请开放 / 基金·敬请开放）、交易（策略回测 / 实盘导入）、AI 研究（模拟交易 / 智能体 / 实验室·敬请开放 / Wiki·敬请开放），看板 / 订阅 / 设置保留直链；点击顶部 **deepAStock 徽标直达看板**；移动端底栏精简为 看板 / 自选 / 模拟 / 订阅 / 设置。
- 新增**占位页**：宏观数据 `/macro`、基金 `/fund`、实验室 `/lab`、知识库 `/knowledge`（敬请开放）。
- **设置页新增「关于」**：展示应用名与当前版本、内置更新日志（读取根目录 `CHANGELOG.md`），并移除设置页重复的 RSSHub 订阅栏位（订阅消息页已具备同功能）。
- 前端大盘 / 自选 / 轮询逻辑重构为 **Pinia store** 统一管理，页面切换与数据刷新更稳定。

## v1.1.4 (2026-09-11) — 本机一键启动 + 微博 Cookie 移到应用内配置

### 新增功能
- **`start.bat` 一键本地启动前后端（无需 Docker）**：后端从 8000、前端从 5173 起自动寻找空闲端口（冲突自动顺延），`Ctrl+C` 一并停止；不再需要原来的菜单式启动器。
- **微博订阅 Cookie 不再写进 docker/环境变量**：新增「设置 → RSSHub 订阅」和「订阅消息 → RSSHub 配置」的**微博 Cookie** 输入框，保存在应用自身配置中；微博用户订阅（`/weibo/user/{uid}`）由后端**直连 `m.weibo.cn`** 拉取（带 Cookie、解析图片/转发/时间），完全不依赖 RSSHub 的 `WEIBO_COOKIES`；Cookie 失效时轮询状态给出明确提示。

### 变更
- 移除 `docker-compose.yml` 中 RSSHub 的 `WEIBO_COOKIES` 注释占位。
- 前端 vite 代理目标改为读取 `BACKEND_PORT` 环境变量，跟随一键启动选出的后端端口。

## v1.1.3 (2026-09-11) — RSSHub 可访问性修复 + 微博订阅配置指引

### 修复
- **RSSHub 仅监听 127.0.0.1，宿主机 11200 端口一直打不开**：compose 中 `LISTEN_INADDR_ANY=0`（已改为 `true` 并重建容器）。自检：`docker exec deepastock-rsshub cat /proc/net/tcp` 无 `0100007F:04B0` 行、`docker port deepastock-rsshub` 显示 `0.0.0.0:11200`。

### 使用须知（微博订阅）
- 微博**个人博主 / 热搜**类路由在 RSSHub 中标记 `requirePuppeteer`：未提供 Cookie 时返回 `Cooling down before new visitor Cookies from https://m.weibo.cn/ may be fetched`（503）。
- 正确姿势：登录 `https://m.weibo.cn` → F12 → 复制任一 `m.weibo.cn` 请求的 **Cookie 请求头整串** → 填入 `docker-compose.yml` rsshub 服务的 `WEIBO_COOKIES`（已预留注释位）→ `docker compose up -d rsshub` 重建 → 测试 `http://127.0.0.1:11200/weibo/user/{uid}`。
- 「设置 → RSSHub 订阅」与「订阅消息」页均已补充该配置提示。

## v1.1.2 (2026-09-11) — 实盘导入支持券商交割单

### 新增功能
- **实盘导入直接解析券商交割单**（CSV/Excel）：
  - **同花顺 / 东方财富 / 同花顺投资账本 / 各券商**导出的交割单，表头别名自动识别（成交日期、证券代码、买卖标志、成交数量、成交价格、成交金额、手续费、印花税、过户费、收付金额…）；
  - 编码自动识别（UTF-8 / GBK 等），**Excel .xlsx/.xls 直接支持**（含标题行、汇总行、日期序列号处理）；
  - 分红 / 股息 / 申购配号 / 资金划转等**非买卖行自动跳过**；佣金 + 印花税 + 过户费自动合计为费用；无价格时按 金额÷数量 补算；
  - 新接口 `POST /api/v1/trade/import/file`（旧 `/import/csv` 保留兼容），返回 导入条数 + 跳过条数。

### 说明
- 国内券商（同花顺/东财/投资账本）**没有面向个人交割单的官方开放 API**（开放接口仅限行情/资讯）；导入交割单文件的方式安全、合规、一劳永逸，后续批次可扩展「持仓快照对照」「汇总买入成本不改动」等功能。

## v1.1.1 (2026-09-11) — 订阅消息页 / 同步修复 / 实例地址可配

### 新增功能
- **导航「订阅」为独立整页**（与其它页面一致、保留顶部导航栏），页内直接可维护 **RSSHub 配置**（启用轮询开关 + 实例地址 + 保存，与「设置 → RSSHub 订阅」一致）：订阅页布局为统计 + 配置行 + 桌面双栏网格（消息流 + 订阅源状态表）。
- **后台轮询完成后重要消息全局推送**：`MainLayout` 轮询会合并平台新闻与 RSS「重要」(importance=1) 条目，新消息以全局渐变通知提示（本地按标题去重，每条只播一次）。
- **「设置 → RSSHub 订阅」实例地址可编辑**：本机开发填宿主机映射 `http://127.0.0.1:11200`；Docker 容器内自动为 `http://rsshub:1200`。保存后立即生效，「立即轮询」前会自动将已修改的地址先写库。
- 「订阅消息」新增/编辑弹窗支持 **RSSHub 路径 + 完整 URL 二选一**：平台切换自动填充路径模板（如微博 `/weibo/user/{uid}`），「测试订阅地址」同时支持两种写法。

### 修复
- **微博等「配置了但像没同步」的根因**：订阅地址直接填了 `https://weibo.com/u/1645823934` 网页地址，解析器拿到的是 HTML 页面 → 静默返回 0 条（`ok:0`）→ 现已修正该订阅源为 RSSHub 路径 `/weibo/user/1645823934`；且解析器遇到 HTML 网页地址会显式报错（`err:地址返回的是 HTML 网页…，请使用 RSSHub 路径`），不再静默。
- `backend/scripts/run_server.py` 的 `stop` 在 UTF-8 模式下读取 GBK `netstat` 输出崩溃 → 显式 `encoding="gbk", errors="replace"`。

### 说明
- 微博 / 公众号 / 股吧等平台推送必须走**本地 RSSHub 实例**：需先 `docker compose up -d rsshub`（宿主机 `11200` 端口），并在设置里把实例地址指向宿主机映射端口；rsshub 未运行或地址不对时，轮询状态会显示 `err:{连接错误}`（不再静默）。

## v1.1.0 (2026-09-11) — 第二批次：订阅消息 / 系统设置 / 界面优化

### 新增功能
- **RSSHub 订阅消息**：本地自建 RSSHub 实例（docker 中 `diygod/rsshub`，顶栏右侧 11200 调试端口；**不调用公网 RSSHub**）。
  - 平台化订阅源管理：微博（用户/热搜）、微信公众号（搜狗关键词）、东方财富股吧等，支持任意自定义 RSS/Atom/JSON 地址。
  - 轮询限频近实时：默认 30s 调度 + 单源独立 `interval_sec`（≥10s），自动去重（source+guid），消息直接落 SQLite（`rss_sources` / `rss_items` 表），过滤 ST 标题 + 按关键词打重要性，30 天保留 + 每源 500 条上限。
  - **独立「订阅消息」页（`/rss`）**：RSS 栏位消息流（来源/重要度/搜索/ST 过滤、可点开原文），订阅源状态与入库条数，新增/编辑/测试/立即轮询，30s 自动刷新；并**预置 4 个实测可直连解析的公开案例源**（雪球每日热帖 / 钛媒体TMT / IT之家 / 爱范儿，无需 RSSHub 即可拉取），新增订阅时可「案例速选」。
  - 大盘看板「消息滚动」同时展示平台新闻 + RSS 订阅推送（带「订阅·来源」标签），设置页可测试订阅地址 / 立即轮询 / 增删改。
- **系统设置页**（右上导航新增「设置」）：
  - 数据源管理：主源 + 备用数据源 1/2/3 顺序回退（sina/tencent），运行时生效无需重启；`DataSourceManager` 按配置链逐源降级。
  - 数据库配置：展示/修改 `DATABASE_URL`，提供「测试连接」；保存后重启生效。
  - RSSHub 开关与实例地址、轮询间隔。
- **大盘看板四板块等高**：投资日历 / 监管异动 / 消息滚动 / ETF资金流 统一为 2×2 等高卡片（桌面端固定等高 + 内部滚动），不再高低不齐。

### 行为调整
- **模拟交易账户观察池差异化**：观察池（当日涨停/强势候选）改为按 `account.id` 轮转顺序，不同账户不再看到完全相同的一池候选。
- **AI 账户禁止买入 ST / \*ST**：候选池过滤 + `_exec` 买入终审双保险；持仓中的 ST 可正常卖出（T+1 校验不变）。
- **模拟账户四池 Tab 切换**：持仓池 / 追踪池 / 观察池 / 复盘池 改为 Tab 展示，不再堆叠；追踪池支持页面内增删（“输入代码→添加”，存规则 `tracked`）。

### 修复
- `main.py` 导入 `app.api.v1.settings` 遮蔽了 `app.config.settings`，导致启动崩溃（`settings.APP_NAME` AttributeError）→ 改为 `import ... as settings_api`。
- RSS 轮询 `poll_sources` 中 aware 时间与 SQLite 存储的 naive `last_poll` 相减报 TypeError，轮询静默失败 → 统一用 `datetime.now(_CN_TZ).replace(tzinfo=None)` 的 naive 时间比较与写库（含 `_prune` 清理逻辑）。
- **ST 检测正则 `\bST\b` 在中文场景失效**（`ST慧球` T 后直接跟汉字无法触发 word boundary）→ 引擎 `_ST_NAME_RE` 与 RSS 解析器 `_ST_RE` 统一改为 `(?<![A-Za-z0-9])(?:S[*★]?ST|\*?ST)(?![A-Za-z0-9])`，且不误伤 `STO/PEST/600519` 等。
- **系统设置「已覆盖」列表**：用户把某项改回默认值后，`overridden` 仍残留该项 → `snapshot()` 仅列出与 env 默认值不同的覆盖项。

## v1.0.0 (2026-09-11) — 首次发布

### 项目命名
- 项目更名为 **deepAStock（深度A股交易）**：后端 `app.config.APP_NAME`、前端标题/Logo、Docker 容器名、`start.bat` 等全部更新。
- Docker 部署改为**单容器一个镜像跑整个项目**（Nginx 托管前端 + 同容器 uvicorn 反代），开放 `80`(前端) 与 `8000`(接口) 端口；默认 SQLite 零配置可用、可选 PostgreSQL（`docker compose --profile postgres up`）。

### 新增功能
- **个股产业链**：行业板块（主力净额/领涨/涨跌幅）+ 板块成分市值 TOP + 所属概念板块行情；板块自身行情改用 `ulist.np/get` 直取，白酒等不在资金流前 200 的板块也能正确展示。
- **行业对比/排名**：个股所属行业中位数增速/ROE、个股排名、增速 TOP / ROE TOP 榜单。
- **涨跌区间分布图**：大盘看板按 11 档区间柱状分布（≤-9% … ≥+9%）。
- **每日复盘真实数据**：东财龙虎榜（净买额/涨幅/上榜原因）+ 板块主力净流入/流出榜（净额、净占比、涨幅、涨停数、市值龙头、人气票）。
- **复盘任务调度**：交易日 18:00 自动生成当日复盘；未到时间或非交易日显示「待生成」，次日不再展示昨日陈旧复盘。
- **自选股**：批量删除（勾选 + 工具栏）、单条删除、最近查看实时价格、中文名称搜索失败给出明确提示。
- **A股全量名称本地库**：每月自动刷新（腾讯排行抓全量约 4600 条），搜索优先走本地库，中文/代码即时命中。

### 行为调整
- **AI 智能体统一为单智能体运行**：个股分析只跑一次（默认 research，可按 `agent_type` 切换 投研/短线/波段），不再并发 3 个智能体，返回更快更省。
- **板块资金流/ETF 数据源**统一为东财（extra 主力净流入），剔除旧的自选「余额宝 ETF」数据源。
- **个股详情页增强**：资金流 K 线图（主图 + 主力净占比副图）、5日/单日主力/超大单等列、财务同环比同比、毛利率/ROE/BPS/每股经营现金流/分红方案。

### 修复
- `to_standard_symbol` 及前端 `fullSymbol` 对 B股（900→SH、200→SZ）与北交所/老三板映射错误 → 修复为 6→SH、900→SH、0/3/200→SZ、其余→BJ。
- 经济基本面：`State` 个股详情接口与自选股详情口径统一（个股详情真实路由为 `/watchlist?symbol=`）。
- 复盘 `sortedLadder` 用错数据源 → 改为基于已就绪复盘报告计算涨停梯队。
- 前端构建产物过旧导致的页面与后端字段不匹配 → 重新 build 对齐。

### 发布准备（v1.0.0）
- **SECRET_KEY**：默认占位改为留空并自动生成随机密钥、持久化到 `data/.secret_key`（重启保持一致）；生产可用环境变量 `SECRET_KEY` 显式覆盖。
- **DEBUG 默认 `false`**（生产安全默认关闭，本地开发可用 `.env` 打开）。
- **favicon**：新增 `frontend/public/favicon.svg`（K线 主题，随品牌色）。
- **前端分包**：`vite.config.js` 增加 `manualChunks`（vue-vendor / element-plus / echarts / hqchart），厂商包稳定缓存。
- **release 打包**：新增 `build_release.ps1` → 产出 `release/deepAStock-v1.0.0.zip`（Docker 全家桶 + 构建产物 + 文档）。
- **部署主路径切换为 Docker**：`docker compose up -d --build` 一键启动（无需安装 Python/Node/DB）；`start.bat` 仅限本机开发。

### 待优化 / 待完成（Roadmap）
见 README「Roadmap」。

## v0.x (历史开发版本)
- 早期迭代：新浪+腾讯直连真实行情、缠论分析、模拟交易、实盘导入、智能体配置等基础功能成型。
- 本机网络受限期间东财系数据（板块/龙虎榜/新闻/财务）曾如实返回空并提示「数据源受限」，后续网络放开后逐项替换为真实数据。